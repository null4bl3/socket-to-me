image: node:latest

stages:
  - test

before_script:
  - npm install

cache:
  paths:
    - node_modules/

test:lint:
  script:
    - npm run lint

test:run:
  stage: test
  script:
   - npm install
   - NODE_ENV=test node index.js
